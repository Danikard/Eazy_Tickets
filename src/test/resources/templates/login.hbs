{{#partial "content"}}
    <body>
    <div class=" container-fluid">
        <div class="row">
            <div class="col-sm-4"></div>
            <div class="col-sm-4" id="center">
                <h4><a href="/">Home</a></h4>
                <h1>Login</h1>
                {{#if name}}
                    <h5>userName:{{useName}}</h5>
                    <h5>Password:{{password}}</h5>

                {{else}}
                    <p>To log in, please visit the <a href="/">home page</a>.</p>
                {{/if}}
            </div>
        </div>
    </div>
    </body>
{{/partial}}
{{> layout.hbs}}