{{#partial "content"}}
    <body>
    <div class=" container-fluid">
        <div class="row">
            <div class="col-sm-4" id="center">
                <h4><a href="/">Sign up</a></h4>
                <h1>Sign up here</h1>
                {{#if name}}
                    <h5> Name:{{userName}}</h5>
                    <h5>Password: {{Password}}</h5>

                {{else}}
                    <p>To sign up , please visit the <a href="/">home page</a>.</p>
                {{/if}}
            </div>
        </div>
    </div>
    </body>
{{/partial}}
{{> layout.hbs}}
